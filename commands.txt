To open ssh-server in ubuntu

sudo apt update
sudo apt install openssh-server -y

systemctl status ssh

ip a



Run Node application on ubuntu

sudo apt update
sudo apt install nodejs npm -y

node -v
npm -v

If Node version is old (Ubuntu default is usually old), install the latest version:

curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

nodejs application
https://github.com/saadactin?tab=repositories






ubuntu xamp
wget https://downloadsapachefriends.global.ssl.fastly.net/8.2.4/xampp-linux-x64-8.2.4-0-installer.run
chmod +x xampp-linux-x64-8.2.4-0-installer.run
sudo ./xampp-linux-x64-8.2.4-0-installer.run
sudo /opt/lampp/lampp start
sudo mv index.php /opt/lampp/htdocs/




Django application
git clone <project-url>
cd <project-folder>
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt

python manage.py migrate

to run : python manage.py runserver
